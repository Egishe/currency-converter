type: object
required:
  - name
  - errorMessage
  - code
properties:
  name:
    type: string
    example: "scrotis"
    description: External service name
  errorMessage:
    type: string
    example: "not found"
    description: Error message
  code:
    type: integer
    example: 404
    description: Error HTTP code
